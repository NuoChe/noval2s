---
description: "[前端] 组件库复用、状态管理与 UI 变更可追溯性"
globs: "**/*.{ts,tsx,vue,jsx,js,css,scss}"
alwaysApply: false
---

# 【前端】架构感知

执行任务前，先分析项目结构与组件关系，并阅读：

- `docs/ARCHITECTURE.md`（系统架构）
- `docs/COMPONENTS.md`（组件文档，若存在）
- `docs/DESIGN-SYSTEM.md`（设计系统说明，若存在）
- `docs/CHANGELOG.md`（最新变更记录，若存在）

添加功能前：

- 优先分析现有组件库与状态管理模式，避免创建冗余组件
- 优先考虑复用 hooks、contexts 或现有组件
- 架构不清晰时，先梳理组件层次与数据流，再执行修改

# 【前端】变更可追溯性

- **提交**：仅在用户明确要求时创建 commit；commit 信息需清晰描述组件变更和影响范围（不自动提交）
- **UI 重大调整**：可生成变更文档与截图对比说明
- **API 或 props 变更**：提供向下兼容方案或迁移指南
- **任务总结**：每次修改完成后，在回复末尾提供简要任务总结，包括：
  - 修改了哪些组件 / hooks / 状态逻辑
  - 为何这样改
  - 如何验证（含类型检查、页面渲染）
  - 是否需更新组件文档或设计系统说明（由用户手动维护）
