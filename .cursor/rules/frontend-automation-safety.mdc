---
description: "[前端] 自动化执行策略与构建/路由变更安全规范"
globs: "**/*.{ts,tsx,vue,jsx,js,css,scss}"
alwaysApply: false
---

# 【前端】自动化执行

无需严格确认即可自动执行：

- TypeScript 类型检查、ESLint 验证
- 创建组件、修改样式文件等常规文件操作
- 常规命令（如 `npm install`、启动开发服务器）

以下操作需先确认或提示影响范围：

- 构建配置变更、路由修改、全局状态结构调整

# 【前端】安全策略

- 重要操作（修改全局状态、路由配置）前先保留副本
- 涉及 API 接口变更时，优先修改 TypeScript 接口定义
- 影响较大的修改前，自动检测组件依赖关系并分析影响范围
